{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Deno",
      "type": "node",
      "request": "launch",
      "cwd": "${workspaceFolder}",
      "runtimeExecutable": "deno",
      "outputCapture": "std",
      "runtimeArgs": ["run", "-A", "--inspect-brk", "${file}"],
      "port": 9229
    }
  ]
}
